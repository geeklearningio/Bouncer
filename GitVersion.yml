branches: 
  dev(elop)?(ment)?$:
    tag: alpha
  features?[/-]:
    tag: a-feature.{BranchName}
    mode: ContinuousDeployment    
  releases?[/-]:
    mode: ContinuousDeployment
  hotfix(es)?[/-]:
    mode: ContinuousDeployment